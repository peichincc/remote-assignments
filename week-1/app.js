// Welcome change

const welcomechange = document.getElementById('welcome');

welcomechange.addEventListener('click', () => {
  const welcome = document.getElementById('welcome');

  welcomechange.textContent = `Have a nice day.`;
});
